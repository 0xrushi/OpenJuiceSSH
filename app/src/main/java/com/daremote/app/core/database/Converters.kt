package com.daremote.app.core.database

class Converters {
    // Room natively supports Long? types, so these converters are not needed.
    // fun fromTimestamp(value: Long?): Long? = value
    // fun toTimestamp(value: Long?): Long? = value
}
